# AUTOGENERATED BY: modules/GithubWorkflowPlugin.scala (`./mill _.ghGenerate`)
# DO NOT EDIT MANUALLY.
name: 'CI: learningpath-api'
on:
  workflow_dispatch:
    inputs: { }
  push:
    paths:
      - learningpath-api/**
      - common/**
      - network/**
      - mapping/**
      - language/**
      - search/**
      - database/**
  pull_request:
    paths:
      - learningpath-api/**
      - common/**
      - network/**
      - mapping/**
      - language/**
      - search/**
      - database/**
jobs:
  unit_tests:
    uses: ./.github/workflows/ci.yaml
    secrets: inherit
    with:
      subproject: learningpath-api
