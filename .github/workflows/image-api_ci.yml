# AUTOGENERATED BY: modules/GithubWorkflowPlugin.scala (`./mill _.ghGenerate`)
# DO NOT EDIT MANUALLY.
name: 'CI: image-api'
on:
  workflow_dispatch:
    inputs: { }
  push:
    paths:
      - image-api/**
      - common/**
      - network/**
      - mapping/**
      - language/**
      - search/**
      - database/**
  pull_request:
    paths:
      - image-api/**
      - common/**
      - network/**
      - mapping/**
      - language/**
      - search/**
      - database/**
jobs:
  unit_tests:
    uses: ./.github/workflows/ci.yaml
    secrets: inherit
    with:
      subproject: image-api
